{# ✅ Crear objeto en Twig y convertirlo a JSON #}
{% set rates = {
    cop: cop.conversion_rate,
    ves: ves.conversion_rate
} %}


<raffle-form 
    payments='{{ payments | json_encode }}'
    raffle="{{ raffle }}" 
    price="{{ price }}" 
    rateexchange='{{ rates|json_encode }}' />