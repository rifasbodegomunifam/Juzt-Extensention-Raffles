<div class="my-8">
	<div class="px-4 mx-auto md:px-4 max-w-7xl">
		
        {{ snippet('archive-header', { title: "Rifas", posts: posts }) }}

		<div class="grid gap-6 grid-cols-1 lg:grid-cols-{{ section.settings.number_cols }}">
			{% for raffle in posts %}
				{{ snippet('raffle-card', {raffle: raffle}) }}
			{% endfor %}
		</div>
	</div>
</div>
